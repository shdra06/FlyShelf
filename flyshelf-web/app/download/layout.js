'use strict';

export const metadata = {
  title: 'Download FlyShelf',
  description: 'Download the latest stable release of FlyShelf (v7.0.0) for Windows (PC) and Android. Direct download of standalone executable and APK package.',
};

export default function DownloadLayout({ children }) {
  return <>{children}</>;
}
