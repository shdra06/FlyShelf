'use strict';

export const metadata = {
  title: 'Features',
  description: 'Explore the powerful productivity tools packed inside FlyShelf. Features Mica glassmorphic UI, real-time sync, Android overlay ball, Gemini AI table extraction, bulk PDF merging and more.',
};

export default function FeaturesLayout({ children }) {
  return <>{children}</>;
}
