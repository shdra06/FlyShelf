'use strict';

export const metadata = {
  title: 'Privacy Policy',
  description: 'FlyShelf is a privacy-first clipboard manager. Read our complete privacy policies. Zero data collected, zero telemetry, local P2P sync.',
};

export default function PrivacyLayout({ children }) {
  return <>{children}</>;
}
