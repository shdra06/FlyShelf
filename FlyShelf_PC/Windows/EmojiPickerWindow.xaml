<controls:MicaWindow x:Class="FlyShelf.Windows.EmojiPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
        Title="Emoji Picker"
        Width="340" Height="420"
        WindowStartupLocation="CenterScreen"
        SystemBackdropType="Mica"
        Background="Transparent"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="False"
        Deactivated="Window_Deactivated">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" CornerRadius="14" CaptionHeight="0" UseAeroCaptionButtons="False" ResizeBorderThickness="0" />
    </WindowChrome.WindowChrome>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header — draggable from entire row -->
        <Grid Grid.Row="0" Margin="14,12,14,6" Background="Transparent" MouseLeftButtonDown="Header_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <emoji:TextBlock Text="😊" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Emoji Picker" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorPrimary}" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Pin / Unpin toggle button -->
            <ui:Button x:Name="PinBtn" Grid.Column="1" Appearance="Secondary" Padding="6,4" CornerRadius="8"
                       Click="Pin_Click" ToolTip="Pinned (always on top)" Margin="0,0,4,0"
                       Foreground="{DynamicResource TypeDefaultColor}">
                <ui:Button.Icon><ui:SymbolIcon x:Name="PinIcon" Symbol="Pin24" FontSize="12"/></ui:Button.Icon>
            </ui:Button>

            <!-- Close button -->
            <ui:Button Grid.Column="2" Appearance="Secondary" Padding="6,4" CornerRadius="8" Click="Close_Click" ToolTip="Close" Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorTertiary}">
                <ui:Button.Icon><ui:SymbolIcon Symbol="Dismiss24" FontSize="12"/></ui:Button.Icon>
            </ui:Button>
        </Grid>

        <!-- Search Box -->
        <Border Grid.Row="1" Margin="14,4,14,8" CornerRadius="10" Padding="0">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{DynamicResource MicaWPF.Brushes.SubtleFillColorSecondary}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource MicaWPF.Brushes.ControlStrokeColorDefault}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Style.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MicaWPF.Brushes.SubtleFillColorTertiary}"/>
                            <Setter Property="BorderBrush" Value="#306366F1"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <StackPanel x:Name="EmojiSearchPlaceholder" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False">
                    <ui:SymbolIcon Symbol="Search24" FontSize="13" Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorTertiary}" Margin="0,0,6,0"/>
                    <TextBlock Text="Search emojis..." Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorTertiary}" FontSize="12"/>
                </StackPanel>
                <TextBox x:Name="EmojiSearchBox" Background="Transparent" BorderThickness="0" 
                         Foreground="{DynamicResource MicaWPF.Brushes.TextFillColorPrimary}" 
                         FontSize="12" Padding="12,8" CaretBrush="#6366F1"
                         TextChanged="EmojiSearchBox_TextChanged"/>
            </Grid>
        </Border>

        <!-- Category Tabs -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="14,0,14,6">
            <StackPanel x:Name="CategoryTabs" Orientation="Horizontal">
                <!-- Populated in code-behind -->
            </StackPanel>
        </ScrollViewer>

        <!-- Emoji Grid -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="6,0,6,8">
            <ItemsControl x:Name="EmojiGrid">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="38" Height="38" Margin="2" Padding="0" Click="Emoji_Click" ToolTip="{Binding Name}" Cursor="Hand" Tag="{Binding Emoji}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="EBorder" Background="Transparent" CornerRadius="8" Padding="0">
                                                    <emoji:TextBlock Text="{Binding Emoji}" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="EBorder" Property="Background" Value="{DynamicResource MicaWPF.Brushes.SubtleFillColorTertiary}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="EBorder" Property="Background" Value="{DynamicResource MicaWPF.Brushes.SubtleFillColorSecondary}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

</controls:MicaWindow>
